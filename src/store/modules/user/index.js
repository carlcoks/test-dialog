import * as TYPES from './types'

const namespaced = true;

const state = () => ({
  user: {
    name: 'vasya'
  }
})

const actions = {
}

const mutations = {
}

const getters = {}

export default {
  namespaced,
  actions,
  state,
  mutations,
  getters
}
